import styled from "styled-components";

export const QuestionWraper = styled.div`
    display: grid;
    gap: 10px;
    grid-template-columns: auto 1fr auto;
    min-height: 80px;
    align-content:center;
    justify-content: center;
    background: #dfdfdf;
    margin:10px 20px;
    padding: 20px;
    border-radius: 8px;
`